MBUS solves the problems of integration of the system components and provides an API for integration with client applications (Web, Android) through the compact protocol.

MBUS решает задачи интеграции компонентов системы и предоставляет API для интеграции с системой клиентских приложений (Web, Android) через компактный протокол обмена.
