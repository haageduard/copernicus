MBUS solves the problems of integration of the system components and provides an API for integration with client applications (Web, Android) through the compact protocol. MBUS implements IPC Sockets layer.
